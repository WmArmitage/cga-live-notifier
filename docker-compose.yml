services:
  notifier:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./config.yml:/app/config.yml:ro
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
